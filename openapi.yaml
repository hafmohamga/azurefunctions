openapi: 3.0.1
info:
  title: APIM Function App
  version: '1.0'
servers:
  - url: 
paths:
  /:
    get:
      summary: HttpTriggerFn
      operationId: get-httptriggerfn
      responses:
        '200':
          description: 
    post:
      summary: HttpTriggerFn
      operationId: post-httptriggerfn
      responses:
        '200':
          description: 
components:
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      name: Ocp-Apim-Subscription-Key
      in: header
    apiKeyQuery:
      type: apiKey
      name: subscription-key
      in: query
security:
  - apiKeyHeader: [ ]
  - apiKeyQuery: [ ]
